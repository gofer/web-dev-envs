server {
  listen                80;
  listen           [::]:80;
  server_name      internal.ca.localdomain;

  location / {
    root           /var/www/crl;
    index          inter_ca.crl;
  }

  location = / {
    rewrite ^(.*)$ http://ca.localdomain/inter_ca.crl redirect;
  }
}
